/*
 * DEPRECATED — The DiRT v2 pipeline now takes pre-downloaded paired-end
 * FASTQ files directly via samples.csv (sample,fastq_1,fastq_2).
 *
 * If you still need to download SRA data, run the helper script
 * `bin/optional_sra_download.sh` BEFORE the pipeline, then point
 * samples.csv at the extracted FASTQ files.
 */
